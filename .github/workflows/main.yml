name: Macsigalla Actions
on: workflow_dispatch
jobs: 
  Testing:
    runs-on: ubuntu-latest
    steps:
      - name: code checkout
        uses: actions/checkout@v4

      - name: mvn test
      - run: mvn test
 
      - name: Checkstyle
        run: mvn checkstyle